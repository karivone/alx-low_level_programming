#include "main.h"
/**
 * mul - a function that multiplies two integers
 * @a: the first input
 * @b: the second input
 * Return: Multiplication result of the two inputs
 */
int mul(int a, int b)
{
	return (a * b);
}
